# This repo will contain most how-tos on devops related:

Added:
- jenkins directory:
  - jenkins ubuntu setup
  - jenkins ubuntu setup bash script
  - jenkins rhel setup
  - jenkins rhel setup bash script